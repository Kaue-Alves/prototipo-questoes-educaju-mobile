# API Configuration
# For Android emulator, use 10.0.2.2 instead of localhost
# For iOS simulator, use localhost or 127.0.0.1
# For physical devices, use your machine's IP address on the local network
EXPO_PUBLIC_API_BASE_URL=http://10.0.2.2:3001

# Production API URL (used when __DEV__ is false)
EXPO_PUBLIC_API_BASE_URL_PROD=https://your-production-api.com

# API Endpoints
EXPO_PUBLIC_API_ENDPOINT_QUESTIONS=/api/questions/generate
